---

cinder_service_user: cinder
cinder_service_user_password: "Ooveem4Od2deif8uog7u"
cinder_service_user_email: "webunit-root@athabascau.ca"
cinder_service_mysql_user: cinder
cinder_service_mysql_user_password: "Ni1AhcheiChei8xe4mui"

cinder_service_region: "{{ default_region }}"

## Best set up inside inventory for the group:
# cinder_service_admin_ip: "{{ ansible_default_ipv4.address }}"
# cinder_service_public_ip: "{{ ansible_default_ipv4.address }}"
# cinder_service_internal_ip: "{{ ansible_default_ipv4.address }}"

## Best set up inside inventory for the host:
# cinder_service_iscsi_ip: "{{ ansible_default_ipv4.address }}"

